[project]
name = "mcp-resources"
version = "0.1.0"
description = "Add your description here: This example realted to adding resources for MPC server."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.25.0",
]
